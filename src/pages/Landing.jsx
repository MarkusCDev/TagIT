import React from 'react'
import { useUserAuth } from '../components/UserAuth';

const Landing = () => {
  
  return (
      <div class="flex h-screen bg-violet-400">
        landing
      </div>
  );
}

export default Landing